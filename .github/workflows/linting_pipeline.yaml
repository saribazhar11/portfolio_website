name: Linting Pipeline

# Trigger on pushes and PRs targeting develop
on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      # 1) Check out your code
      - uses: actions/checkout@v3

      # 2) Set up Node.js environment
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      # 3) Install linting tools
      - name: Install HTMLHint & Stylelint
        run: npm install --save-dev htmlhint stylelint stylelint-config-standard

      # 4) Run HTMLHint against all .html files
      - name: HTMLHint
        run: npx htmlhint "**/*.html" --config .htmlhintrc.json

      # 5) Run Stylelint against all .css files
      - name: Stylelint
        run: npx stylelint "**/*.css" --config .stylelintrc.json --allow-empty-inputs
      # 6) Check for any linting errors
      - name: Check for linting errors
        run: |
          if [ $? -ne 0 ]; then
            echo "Linting errors found. Please fix them before merging."
            exit 1
          else
            echo "No linting errors found."
          fi
      # 7) Upload linting results as artifacts
      - name: Upload linting results
        uses: actions/upload-artifact@v3
        with:
          name: lint-results
          path: |
            **/*.html
            **/*.css
            .htmlhintrc.json
            .stylelintrc.json
          retention-days: 7
      # 8) Notify on success or failure
      - name: Notify on success or failure
        if: failure()
        run: echo "Linting failed. Please check the results."
        else:
          run: echo "Linting passed successfully."
